[package]
name = "ai-engine-graph"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ai-engine-core = { path = "../ai-engine-core" }
